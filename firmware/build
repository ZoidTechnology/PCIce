#!/bin/bash

set -e

avr-gcc -Wall -Os -mmcu=attiny13 -o output.elf *.c

avr-objcopy -O ihex output.elf output.hex

avrdude -p attiny13 -c avrispmkii -U flash:w:output.hex

rm output.elf
rm output.hex